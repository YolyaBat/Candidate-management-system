import CircularProgress from '@mui/material/CircularProgress';
import { SpinnerWrapper } from './index.css';

const Spinner = () => (
	<SpinnerWrapper>
		<CircularProgress />
	</SpinnerWrapper>
);

export default Spinner;
