import CandidateSummary from '../CandidateSummary';
import { LayoutWrapper } from './index.css';

const Layout = () => {
	return (
		<LayoutWrapper>
			<CandidateSummary />
		</LayoutWrapper>
	);
};

export default Layout;
